{
  "name": "iran-food",
  "version": "1.0.0",
  "private": true,
  "description": "Iran Food — visual dataset landing page with upload API and admin panel",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c green,cyan \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "concurrently -n backend,frontend -c green,cyan \"npm run start --prefix backend\" \"npm run preview --prefix frontend\"",
    "seed": "npm run seed --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
